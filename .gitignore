# --- ESP-IDF build output ---
build/

# --- Desktop simulator build output (port/pc/CMakeLists.txt) ---
build-sim/
sdkconfig
sdkconfig.old
sdkconfig.ci
sdkconfig.defaults.old

# Resolved by the component manager from main/idf_component.yml.
# Both are regenerated by `idf.py reconfigure`; the lock file is deliberately
# not tracked so a fresh checkout always re-resolves against the manifest.
managed_components/
dependencies.lock

# --- Editors / tooling ---
.idea/
.cache/
compile_commands.json
*.swp
*~

# Screenshots the run skill and the VS Code tasks drop in the project root.
panel.png
sim.png

# --- Serial / debug artefacts ---
*.core
core.*

# --- Generated / vendored tooling ---
tools/node_modules/
tools/package*.json

# Byte-compiled Python from the run skill.
__pycache__/
*.pyc
