# 16 MB flash. Two app slots so the firmware can be replaced over the air, and
# a settings partition of our own so option blobs never share space with the
# Wi-Fi calibration data IDF keeps in the default `nvs`.
#
#   nvs        24 kB   IDF's own: Wi-Fi and PHY calibration
#   phy_init    4 kB
#   otadata     8 kB   which app slot boots next
#   settings   24 kB   option blobs, one NVS entry per option::Key
#                      -- see main/Settings.cpp
#   ota_0       4 MB   the app is ~1.1 MB today
#   ota_1       4 MB
#   storage  7.88 MB   unused so far
#
# `settings` is small on purpose, and the reason is internal RAM rather than
# flash. Mounting an NVS partition costs internal RAM that is never given back,
# and three things on this board each need a *contiguous* 32 kB of it: the LVGL
# task, the console task and the CAN thread.
#
# CONFIG_NVS_ALLOCATE_CACHE_IN_SPIRAM=y moves NVS's page cache and key hash
# list to PSRAM, but it does not move everything -- measured on this board,
# free internal RAM after boot:
#
#   settings not mounted   ->  85 kB free, largest block 31.7 kB
#   settings    24 kB      ->  73 kB free, largest block 31.7 kB
#   settings   184 kB      ->  45 kB free, largest block 23.5 kB
#
# So mounting still costs ~12 kB at 24 kB and ~40 kB at 184 kB, roughly 700 B
# per extra 4 kB sector. Without the PSRAM option the 184 kB case was worse
# still: the CAN thread could not be created and the board aborted at boot with
# "pthread: Failed to create task".
#
# 24 kB is the same size IDF gives its own `nvs` and holds 756 entries; the
# option blobs use 17. Do not enlarge it without re-reading app_main's
# "largest block" line afterwards.
#
# The table itself sits at 0x10000, not IDF's default 0x8000. The second-stage
# bootloader loads at 0x2000, so the default leaves it 0x6000 bytes -- and
# IDF v5.5.5 builds a 0x6120-byte bootloader for the P4, which fails the build
# with "Bootloader binary size ... too large for partition table offset".
# At 0x10000 the bootloader may grow to 0xE000. Moving the table is what makes
# the offsets below start at 0x11000; the data partitions then fill the
# 0x11000..0x20000 gap exactly, because ota_0 has to start on a 64 kB boundary.
#
# Changing this offset invalidates everything already in flash: the old nvs and
# settings partitions are at addresses nothing reads any more. Erase once
# (`idf.py -p /dev/ttyACM0 erase-flash`) before the first flash; `Settings`
# repopulates the option blobs on the next boot.
#
# Offsets are spelled out because a mistake here bricks the board quietly.
# gen_esp32part.py rejects a trailing comment on a partition line, hence the
# table above.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x11000,   0x6000,
phy_init,   data, phy,      0x17000,   0x1000,
otadata,    data, ota,      0x18000,   0x2000,
settings,   data, nvs,      0x1a000,   0x6000,
ota_0,      app,  ota_0,    0x20000,   0x400000,
ota_1,      app,  ota_1,    0x420000,  0x400000,
storage,    data, spiffs,   0x820000,  0x7e0000,
